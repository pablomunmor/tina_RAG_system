# To use the application, rename this file to .env and add your API keys.

# Optional: For using local models via HuggingFace (the default setting)
# Get a token from https://huggingface.co/settings/tokens
HUGGING_FACE_HUB_API_TOKEN=""

# Optional: For using OpenAI models
# Get an API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=""

# Optional: For persistent storage on deployment platforms like Render
# Set this to the mount path of your persistent disk (e.g., /var/data)
STORAGE_DIR=""

# --- Application Security ---
# Set these to enable password protection for the web interface.
APP_USER="admin"
APP_PASSWORD=""

# --- Optional Technical flags ---
# Set this to TRUE to avoid errors on some systems with conflicting libraries (e.g. OMP: Error #15)
KMP_DUPLICATE_LIB_OK="TRUE"